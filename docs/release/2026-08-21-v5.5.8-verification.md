# v5.5.8 macOS 启动器 Miku Logo 修复验证记录

## 问题与根因

- macOS「HeiGe 皮肤启动器」的 Finder 图标已经使用初音未来人物，但内置面板左上角仍由 `BrandLogoView` 绘制渐变色块和文本「H」。
- 这是原生 AppKit 面板中的临时占位实现，与安装包中的 `AppIcon.icns` 没有建立资源关系。

## 修复边界

- `BrandLogoView` 改为使用原生 `NSImageView`。
- 优先读取 APP Bundle 中的 `AppIcon.icns`，异常时回退到应用自身图标。
- 图片使用等比例缩放，保留圆角和细描边，不更改窗口布局、产品卡片和操作逻辑。
- Codex 与 WorkBuddy 的字母标记继续作为产品卡片识别，不属于主品牌 Logo。

## 自动化验证

- 新增回归契约：必须读取 Bundle 中的 `AppIcon.icns`，必须使用 `NSImageView` 和等比例缩放，禁止恢复 `labelWithString: "H"`。
- 聚焦启动器、安装与版本契约：147 项通过，0 项失败。
- 完整 `npm run release:check`：1113 项，1107 项通过，0 项失败，6 项跳过。
- 视觉资源来源检查：42 项通过。
- `.skill` SHA-256：`2bab9acf665079d6313b317407f347d74ecb976597d759316399471f1225e9d7`。
- 归档包含 `AppIcon.icns`，原生启动器二进制保留可执行位。

## 本机运行态验证

- 稳定运行时：`/Users/blakexu/.codex/heige-codex-skin-studio`，版本 5.5.8。
- 桌面启动器：`/Users/blakexu/Applications/HeiGe 皮肤启动器.app`，版本 5.5.8，Schema 4。
- 旧进程正常退出后重新打开，新窗口左上角显示初音未来人物头像。
- macOS 辅助功能树将该元素识别为「图像 初音未来主题图标」，不再出现文本「H」。
- Codex 与 WorkBuddy 两张卡片均完成状态加载，最近皮肤和操作按钮正常显示。
