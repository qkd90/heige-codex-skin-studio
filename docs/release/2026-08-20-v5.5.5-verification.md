# v5.5.5 双产品 macOS 启动器验证记录

日期：2026-08-20

## 交付范围

本版把本地「HeiGe 皮肤启动器」从 Schema 3 Codex 单产品 shell 入口升级为 Schema 4 原生 AppKit 双产品面板。面板只选择 Codex 或 WorkBuddy，并自动恢复所选产品最近一次使用的非原生皮肤。

## 自动化验证

执行：

```bash
umask 022
npm run release:check
```

结果：1112 项测试，1106 通过，0 失败，6 项按平台条件跳过；发布素材来源门禁接受 42 个视觉素材。

本机登录 shell 的默认 umask 是 077，旧版部分 legacy fixture 假设 022。发布验证显式使用 CI 与历史发布一致的 022；生产安装器自身另有 077、权限归一和私有状态目录测试。

原生启动器构建验证：

```text
Architectures in the fat file: assets/launcher/HeiGeSkinLauncher.bin are: x86_64 arm64
```

## 发布包

文件：`output/heige-codex-skin-studio.skill`

SHA-256：`8d814dff267029ac0684336b1ed60f1b549ec78a858c84de1c945d406d26734b`

归档已确认包含：

- `payload/assets/launcher/HeiGeSkinLauncher.bin`
- `payload/native/macos-launcher/main.swift`
- `payload/scripts/launch-skin.command`
- `payload/scripts/launcher-state.command`
- `payload/src/launcher-panel-state.mjs`
- v5.5.5 `payload/package.json`

## 本机覆盖安装

覆盖前：稳定运行时 v5.5.4，桌面启动器 v5.5.4，Schema 3。

执行项目自带 `scripts/install.command`，事务结果为 `decision=commit`、`recovered=false`。覆盖后：

- 稳定运行时：v5.5.5。
- 启动器 Bundle：v5.5.5，Schema 4。
- 路径：`/Users/blakexu/Applications/HeiGe 皮肤启动器.app`。
- `codesign --verify --deep --strict`：通过。
- 安装后二进制：x86_64 与 arm64 universal。
- 安装图标 SHA-256 与发布源一致：`5946b2548071c7b06f2567e3081babd1f6dbe534ea69b172b69c68352f46afff`。
- Spotlight 可按 `com.heige.codex-skin-launcher` 找到唯一目标 APP。

## 真实界面与注入验收

启动器实际窗口已确认：

- 原生单窗口，无 Terminal 窗口。
- Codex 与 WorkBuddy 两张并列卡片。
- Codex 显示最近皮肤「大佬 · 点烟」和「当前会话」。
- WorkBuddy 显示最近皮肤「Miku 488137」和「一次性皮肤」。
- 显示 9341、9342 隔离说明和「查看诊断与日志」。

真实点击结果：

- Codex：点击后启动器退出；9341 主 renderer 状态为 `installed=true`、`mode=active`、`themeId=dalao-dianyan`、`menu=true`、`persistenceEnabled=false`，无失败目标。Computer Use 出于产品安全策略禁止读取 Codex 自身窗口，因此没有独立截图证据。
- WorkBuddy：点击后启动器退出并前置 WorkBuddy；9342 主 renderer 状态为 `installed=true`、`mode=active`、`themeId=miku-488137`、`menu=true`、`persistenceEnabled=false`，无失败目标。实际窗口可见 Miku 488137 初音未来背景和顶部主题入口。
- `launchctl` 未发现本工具创建的 WorkBuddy 常驻项。

## 残余边界

- universal binary 已静态验证两个架构，本轮没有 Intel Mac 实机执行证据。
- 本地 Bundle 是 ad hoc 签名，不是 Apple Developer ID 签名或公证。
- 本轮未推送 GitHub、Tag 或 Release。
