# HeiGe Codex Skin Studio 5.5.13 验证记录

日期：2026-08-21

## 本轮范围

- macOS 启动器的 Codex 与 WorkBuddy 卡片增加「打开皮肤」和「关闭皮肤」。
- 启动器底部增加「一键修复」，对已安装产品执行强制干净重启并恢复最近皮肤。
- 关闭操作只暂停当前会话，保留最近主题与常驻选择。

## 实现边界

- Codex 固定使用 9341，WorkBuddy 固定使用 9342。
- 打开、关闭、修复分别走版本绑定的 `launcher-apply`、`launcher-close`、`launcher-repair`。
- 新增 `close-skin.command` 与 `repair-skin.command` 固定包装脚本。
- 不修改 `app.asar`，不联网下载，不提权，不创建登录项，不静默改变常驻状态。

## 自动化与构建

- 完整发布门禁：1124 项测试，1118 通过，0 失败，6 项按平台条件跳过。
- 发布素材来源门禁接受 43 个视觉素材。
- 原生启动器架构：`x86_64 arm64`。
- 安装包 SHA-256：`829b2f8b52cd32d9d886ca542a15dbda8b16fcfc89324bec388cedd962f3508c`。

## 本机覆盖安装

- 从最终 `output/heige-codex-skin-studio.skill` 解包并执行包内事务安装器。
- 安装决策：`commit`，`recovered=false`，`persistenceEnabled=false`。
- 稳定运行时：`/Users/blakexu/.codex/heige-codex-skin-studio`，版本 5.5.13。
- 启动器：`/Users/blakexu/Applications/HeiGe 皮肤启动器.app`，Bundle 版本 5.5.13，Schema 5。
- `codesign --verify --deep --strict` 通过。
- 安装时设置 `HEIGE_SKIP_APPLY=1`，没有重启或重新注入 Codex 与 WorkBuddy。

## 真实窗口验收

- 安装后的窗口显示 `MAC 专属 · 5.5.13`。
- Codex 卡片显示「打开 Codex 皮肤」和「关闭皮肤」。
- WorkBuddy 卡片显示「打开 WorkBuddy 皮肤」和「关闭皮肤」。
- 底部显示「一键修复」和「诊断与日志」。
- 深色模式下文字、卡片、主按钮、次按钮和底部操作均清晰可见。
- 为避免打断当前任务，视觉验收没有点击会重启产品的「一键修复」；其版本绑定、端口隔离、强制重启和最近主题恢复行为由自动化测试覆盖。

## 发布边界

本轮只完成本地开发、构建、确定性打包、覆盖安装和真实窗口验收。没有推送分支、创建标签或发布 GitHub Release。
