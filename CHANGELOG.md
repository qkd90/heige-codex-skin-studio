# 更新日志

## 5.5.17 - 2026-09-13

### 修复

- 发布用户主题时，若选择提交因状态版本冲突或请求中断而失败，控制器现在会按主题 id 回收已落盘的孤儿目录；清理失败只记录日志，不掩盖原始错误。新增两条回归测试覆盖冲突回收与清理失败路径。
- 迁移测试在浅克隆（对象库缺少历史公开提交 79b03dc）环境下改为跳过并提示补全历史，不再对贡献者误报失败。
- install.command 在安装失败时输出 doctor 与 status 排查命令，并在拉起注入窗口后提示失败时的下一步，闭环错误引导。

### 安全

- macOS 安装就绪确认的控制授权改经子进程 stdin 管道传递，控制 token 不再出现在同用户可读的进程环境变量中；授权 JSON 仍走原有严格 schema 校验。

### 维护

- package.json 固定 packageManager 为 npm@10.9.8，配合既有 lockfile 锁死安装期行为。
- docs/release 新增验证报告归档索引，按日期倒序导航 15 份历史报告。

### 验证

- 完整门禁 1136 项测试，1130 项通过，0 失败，6 项按平台条件跳过。

## 5.5.16 - 2026-09-04

### 修复

- 适配 WorkBuddy 5.5.2、5.5.3 的新版 Renderer：放行启动快照 query，同时继续按完整本地 Renderer 路径校验目标身份，避免升级后无法注入。
- 接管新版 `cr-*` 对话设计令牌，恢复助手回复、用户气泡、代码块、输入框和文字配色，并保留旧版 `cb-*` 兼容规则。
- 修复 `.conversation-shell` 和 `.wb-home-route` 纯白背景遮住主题图的问题，首页与历史对话均恢复主题背景。
- WorkBuddy 皮肤不再生成右下角拍立得自拍照，避免遮挡模型、语音和发送功能键；Codex 端原有效果保持不变。

### 文档

- 将 HeyRoute 专属注册链接与新用户 `US$15` 试用额度说明同步到完整项目索引。

### 验证

- 在 WorkBuddy 5.5.3 当前窗口完成首页、历史对话、输入区和右下角功能区真机验收。
- 完整发布门禁共 1134 项测试，1128 项通过，0 失败，6 项按平台条件跳过；44 个视觉素材通过发布来源检查。

## 5.5.15 - 2026-08-23

### 修复

- 修复 WorkBuddy 5.3.14「允许完全访问」确认框被主题壁纸穿透的问题。宿主通过通用 `[class*="dialogContainer"]` 规则把所有 CSS Modules 确认框强制绑定到透明主画布背景，新版直接对齐该稳定语义选择器并恢复不透明主题底色。
- 通用确认框修复同时覆盖撤销授权、专家召唤等同源对话框，不依赖每次构建都会变化的类名哈希；外层 60% 黑色遮罩保持不变。
- 复核当前 WorkBuddy 实际加载的 87 份样式表，补齐文件管理模块的类型选择和上传选择两个下拉菜单实底规则。

### 验证

- 当前权限确认框真机读取为不透明 `rgb(245, 246, 252)`，外层遮罩仍为 `rgba(0, 0, 0, 0.6)`，取消按钮可用，确认按钮在未勾选风险确认前保持禁用。

### 文档

- 在中英文项目介绍页新增 HeiGe 皮肤启动器 5.5.15 真机截图和更新声明，展示 Codex、WorkBuddy 独立打开与关闭皮肤、一键修复、诊断日志及夜间模式。

## 5.5.14 - 2026-08-21

### 修复

- 修复启动器包装脚本在 GNU `mktemp` 环境下无法创建临时文件的问题，继续兼容 macOS BSD `mktemp`。
- 为打包内的 Swift 源码固定 LF 行尾，避免 Windows checkout 改写字节后造成跨平台发布包哈希漂移。

### 验证

- 新增可移植临时文件模板与 Swift 行尾契约测试，并要求 Linux、macOS、Windows 和可复现安装包四组 GitHub Actions 全部通过。

## 5.5.13 - 2026-08-21

### 新增

- macOS「HeiGe 皮肤启动器」的 Codex 与 WorkBuddy 卡片新增明确的「打开皮肤」和「关闭皮肤」操作。关闭只暂停当前会话，不删除主题、不改变最近主题，也不修改常驻选择。
- 启动器底部新增「一键修复」。它会跳过未安装产品，对已安装的 Codex 与 WorkBuddy 分别走 9341、9342 隔离端口，执行干净重启并自动恢复各自最近皮肤。

### 安全与验证

- 新操作只经过版本绑定的 `launcher-close`、`launcher-repair` 与固定包装脚本，不接收任意路径，不修改 `app.asar`，不联网下载，也不静默改变常驻状态。
- 新增 CLI、产品路由、脚本归档和 AppKit 界面契约测试，并重新编译 `x86_64 arm64` 双架构程序。

## 5.5.12 - 2026-08-21

### 优化

- 重做 macOS「HeiGe 皮肤启动器」视觉：窗口升级为 Miku 数字终端风格，增加青粉顶部光带、柔和背景光晕、状态胶囊和更清晰的卡片层级，同时保持 macOS 深浅色语义色适配。
- Codex 与 WorkBuddy 卡片不再显示「C」「W」字母占位图，改为通过 `NSWorkspace` 读取本机真实 APP 图标；未安装时才使用对应的 SF Symbol 回退。
- 最近皮肤、产品模式、独立端口、安全恢复状态和诊断入口重新排版，主操作按钮使用产品强调色，但恢复逻辑、9341 与 9342 端口隔离及常驻边界不变。

### 验证

- 新增真实 APP 图标和视觉结构回归测试，重新编译 `x86_64 arm64` 双架构程序，并从最终安装包完成深色模式真机验收。

## 5.5.11 - 2026-08-21

### 修复

- 修复启动器窗口标题区域仍复用 Dock `AppIcon.icns`、导致内置 Logo 看起来没有更换的问题。窗口现在独立加载 `LauncherLogo.png`，使用皮肤壁纸核心人物的完整上半身初音未来构图；Finder 与 Dock 图标保持不变。
- 启动器 Bundle 升级到 Schema 5，将窗口 Logo 纳入固定资源白名单、PNG 结构校验、权限校验和 ad hoc 签名完整性校验；现有 Schema 4 安装可以直接覆盖升级。

### 验证

- 新增独立 Logo 加载、打包资源、Schema 4 升级和签名后 Logo 篡改拒绝测试，并重新编译 `x86_64 arm64` 双架构程序。

## 5.5.10 - 2026-08-21

### 修复

- 修复 macOS 从浅色切换到深色外观后，皮肤启动器文字已经变亮、卡片与窗口 Layer 背景却仍停留在旧颜色，导致内容看不清的问题。
- 窗口、产品卡片、最近皮肤区域和 Logo 边框统一改为外观感知表面，在 `viewDidChangeEffectiveAppearance` 时重新解析 macOS 语义色；标题与主题名明确使用系统主文字色，按钮继续使用原生自适应样式。

### 验证

- 新增原生启动器深浅色切换回归测试，并重新编译 `x86_64 arm64` 双架构程序。

## 5.5.9 - 2026-08-21

### 修复

- 修复 WorkBuddy 5.3.14 任务归档确认窗被主题壁纸穿透的问题。宿主用更高特异性的 `!important` 规则把卡片背景绑定到透明的 `--wb-bg-primary`，新版通过稳定的 `.conversation-archive-dialog` 父类只恢复卡片实底，保留外层 60% 黑色遮罩。
- 修复通用遮罩兜底只在 overlay class 恰好排在最后时生效的问题；现在同一节点带多个 class 时也能命中，避免专家团一类弹层再次漏底。
- 审计 WorkBuddy 当前加载的 85 份 CSS，筛查 105 个透明主背景候选，排除按钮、输入框、滚动条和加载遮罩后，将反馈、协作、连接器、文件管理、技能、文档选择器等稳定浮层根容器纳入实底名单。

### 验证

- 当前「归档任务」弹窗真机验证：卡片背景为不透明 `rgb(245, 246, 252)`，遮罩保持 `rgba(0, 0, 0, 0.6)`，取消和确认归档按钮均保留。

## 5.5.8 - 2026-08-21

### 修复

- 修复 macOS「HeiGe 皮肤启动器」内置面板左上角仍显示渐变字母「H」占位图的问题。
- 面板品牌 Logo 现在直接读取 APP Bundle 中的 `AppIcon.icns`，与 Finder、Dock 使用同一份初音未来人物图标；保留圆角和细描边，并使用等比例缩放避免人物变形。

### 验证

- 原生 AppKit 回归测试明确禁止「H」占位图，并验证 Bundle 图标加载和 `NSImageView` 等比例缩放契约。

## 5.5.7 - 2026-08-21

### 修复

- 修复 WorkBuddy 5.3.14「专家团」详情弹层被主题壁纸穿透的问题。弹层原生读取 `--wb-bg-primary`，而皮肤为主画布透出壁纸会把该令牌设为透明，导致后方专家卡片和文字叠进弹层正文。
- 将稳定类名 `.ec-modal-card` 纳入实底浮层名单，只恢复弹层卡片背景，不改变主画布透明效果，也不改变外层 60% 黑色遮罩。

### 验证

- 在当前打开的「内容创作专家团」弹层中真机验证：卡片背景为不透明 `rgb(245, 246, 252)`，遮罩仍为 `rgba(0, 0, 0, 0.6)`，关闭按钮、召唤按钮和正文滚动区均保留。

## 5.5.6 - 2026-08-20

### 修复

- 修复 WorkBuddy 长对话里出现大量横向空白条的问题。宿主会把一条 AI 回复拆成多个同 request-id 的消息分片，未使用分片只有空内容节点；旧皮肤把阅读蒙版和垂直内边距直接加在每个消息外壳上，导致空分片被撑成 28px 高的卡片。
- 阅读蒙版现在只落在非空的直接内容节点上，空分片恢复为 0 高度，同时保留正文、工具栏和复杂内容的主题色蒙版。选择器继续只使用稳定语义类名，不依赖 WorkBuddy 构建哈希，也不使用 `:has()`。
- 修复 `.skill` 归档遗漏原生启动器二进制可执行位的问题，确保用户从发布包解压后可以直接运行事务安装器完成覆盖安装。

### 验证

- 在 WorkBuddy 5.3.14 的「香港服务器巡检」长对话中复现并验证：58 个消息分片里有 51 个空分片，修复前单个高度 28px，修复后全部为 0；正常回复正文、操作栏和输入区仍可见。

## 5.5.5 - 2026-08-20

### 新功能

- macOS「HeiGe 皮肤启动器」升级为原生双卡片面板，可分别恢复 Codex 与 WorkBuddy 最近一次使用的非原生皮肤。
- 启动器显示两个产品的安装状态、最近皮肤和一次性或当前会话语义；未安装卡片禁用，失败可原位重试，成功后前置目标 APP。
- 随包提供 arm64 与 x86_64 universal AppKit 二进制，普通用户不需要安装 Xcode。

### 安全与兼容

- 生成 Bundle 升级为 Schema 4，继续沿用原子覆盖安装、崩溃恢复、ad hoc 签名和 LaunchServices 注册，并兼容覆盖旧 Schema 1、2、3 启动器。
- Codex 与 WorkBuddy 使用版本绑定的 `launcher-apply`，端口、状态、锁和日志保持隔离；WorkBuddy 仍不启用常驻或 `file://` 控制通道。

## 5.5.4 - 2026-08-20

### 新功能

- macOS 安装现在生成 Schema 3「HeiGe 皮肤启动器」，包含 Miku 图标、本地 ad hoc 完整性签名、LaunchServices 注册和可见失败提示。
- 点击启动器会启动或受控重启官方 Codex Desktop，以回环 CDP 恢复最近一次非原生主题；没有历史时回退到 `miku-488137`，且不改变用户的常驻选择。
- 启动器可在安全门通过时备份并重建静态损坏的 operation-lock 状态根，然后只重试一次。

### 安全边界

- 启动器只指向稳定安装树，不修改官方 `app.asar`、二进制或签名。ad hoc 签名不等于 Apple Developer ID 签名或公证。

## 5.5.3 - 2026-08-19

### 修复

- 修复 Windows Store/MSIX 把「调试参数已在命令行、端口却连不上」一律收成 `abort-incompatible` 的问题：现在先区分 AppContainer 回环隔离、激活丢参、以及真正没有监听的版本不兼容。商店版等待窗口加长到约 45 秒；AUMID 丢参时回退到已绑定包声明的可执行文件，`WindowsApps` Access Denied 则 fail-closed。
- Windows `doctor` 不再把「有精确监听」当成「命令行已带调试参数」：flag 与 HTTP 可达性解耦，商店包会附带 `loopbackExempt` / `loopbackIsolated`。
- 状态根 ACL：PowerShell `icacls` 回退改为先 `/grant` 再 `/setowner *SID`（本机 `icacls` 不允许把 `/setowner` 和 `/grant` 写在同一条命令里，SID 也必须带 `*` 前缀）；仅含本工具文件且继承了不可信写权限的目录允许改为 protect，而不是把永久 ACL 失败当成瞬时 `LOCK_PERMISSIONS` 重试。未知文件的目录报 `LOCK_ACL_UNTRUSTED`。

### 新功能

- 新增 `scripts/windows/enable-loopback.ps1` / `enable-loopback.bat`：给当前商店版 Codex 添加一次 CheckNetIsolation 回环豁免。只在用户明确同意时提权，apply 不会每次弹 UAC。

### 已知边界

- 商店版端到端注入仍待真机 netstat / `/json/version` 证据；本文不把自动化测试冒充 Store 真机结论。

### 测试

- 新增回环隔离、丢参回退、doctor 解耦、状态根 ACL 重建与 `enable-loopback` 编码/提权契约断言。

## 5.5.2 - 2026-08-11

### 修复

- WorkBuddy 的设置弹层、下拉、右键菜单和各种对话框不再透光。这些浮层自己不画底色，靠的是宿主页面本来是纯白；皮肤为了让主题图透出来把页面底改成透明，它们就跟着一起透了，设置界面背后的对话正文直接印在设置文字上，两层字叠着没法读。半透明现在只留给对话区自己。
- 修法分两层。读令牌的那批浮层（`--wb-bg-modal`、`--wb-bg-popover`、`--cb-dialog-bg`、`--cb-popover-b*`、`--cb-bg-elevated` 等十九处）统一指向新增的实底令牌 `--heige-solid`，它直接取主题底色、不掺任何透明度。什么令牌都不读的那批，按稳定类名显式钉实底，名单三十八项，逐个真机核过。
- 面板自带的小对话框再补一层兜底：按 WorkBuddy 自己的命名规律匹配 `*-modal-overlay`、`*__overlay`、`*-editor-overlay`、`*-dialog-overlay`、`*-confirm-overlay` 这几类模态遮罩，给遮罩的直接子节点垫实底。连字符和 BEM 双下划线两种写法都列了：添加模型框的遮罩叫 `models-settings-panel__editor-overlay`，只写连字符那种会漏掉它。用收尾匹配而不是包含匹配，是为了避开 `*-loading-overlay`、`*-stencil-overlay`、`*-startup-overlay` 这些同样带 overlay 但不是模态遮罩的元素——它们的子节点是转圈图标和图片裁剪框，垫实底反而糊。遮罩本体一律不动，保持半透明黑纱，垫实了整个界面会全黑。
- 顺带治好添加模型框的保存按钮：WorkBuddy 把「编辑区底色」当这个按钮的文字色使（`color: var(--cb-vscode-editor-background)`），皮肤把编辑区底改成透明之后，实心强调色上一个字都看不见。改成走 `--heige-on-accent`。浮层没垫实底之前整块面板都是透的，这个毛病看不出来，垫实了才暴露。
- 修好 WorkBuddy 主窗口识别拒绝 hash 路由的问题：WorkBuddy 用 hash 路由，光是打开一次设置弹层就会把地址变成 `index.html#`，而识别逻辑此前连 `#` 都拒，于是皮肤在正常使用中途突然认不出主窗口，`apply` / `status` / `restore` 全部报「未发现经过严格识别的主窗口 renderer」。现在 fragment 不参与身份判断，身份仍然只看解码后的 pathname，`file:///tmp/evil.html#/WorkBuddy.app/…/index.html` 这类构造照样被后缀检查挡掉。Codex 侧的识别逻辑没动。
- 真机核对自 WorkBuddy 5.3.11，深色（genshin-night）与浅色（miku-488137）两套主题各验一遍：设置弹层、设置左侧导航、用户菜单、添加模型对话框、默认权限下拉、会话列表更多菜单全部实底可读，对话区保持半透明。

## 5.5.1 - 2026-08-11

### 修复

- WorkBuddy 的 AI 回复补上阅读蒙版：回复原生没有任何底色，整段直接压在主题背景图上，深色图和高饱和图上基本读不了。现在阅读增强开着时给整条回复垫一层 90% 主题色表面，做法和 Codex 侧的 `[data-response-annotation-conversation]` 一致，同样不用实时模糊、不加阴影和观察者。
- 同一处的文字颜色一并接管：正文（`rgb(0,0,0)`）、底部图标条、消耗计数和时间（`rgba(0,0,0,.7)`）、以及蒙版外面的发言人名字，颜色都写死在各自节点上、不继承外层容器。只垫蒙版不接管这几处，深色主题下就是黑字压深色蒙版，比不垫更糊。这几条不挂阅读增强开关，因为颜色写死是宿主自己的毛病，关掉蒙版一样要治；名字永远直接压在背景图上、没有底可垫，额外补一圈同底色光晕。
- 挂钩全部取稳定选择器（`.cb-assistant-message`、`.cb-markdown`、`.cb-credit-usage-text`、`.cb-message-time-tip`、`.avatar-container .name`），同节点并存的构建哈希类名一个没碰。真机核对自 WorkBuddy 5.3.11，深色（genshin-night）与浅色（miku-488137）两套主题各验一遍；消息列表是 virtuoso 虚拟滚动，条目高度由它自己的 ResizeObserver 重测，补内边距后 `data-known-size` 从 127 跟到 155，不会错位。

## 5.5.0 - 2026-08-11

### 新增

- 支持给腾讯 CodeBuddy 桌面端（WorkBuddy）换肤：CLI 新增 `--app codex|workbuddy` 开关（环境变量 `HEIGE_SKIN_APP` 为回退），新增 `scripts/workbuddy-apply.command`、`scripts/workbuddy-enable-skin.command`、`scripts/workbuddy-restore.command` 三个稳定入口。默认调试端口 9342，与 Codex 的 9341 互不干扰，可同时开着。
- 新增产品档案层 `src/products.mjs`：宿主应用的身份（bundle id、Team ID、可执行文件名）、端口、调试端口开法、状态目录全部收进档案，上层代码只读档案字段。Codex 侧对外行为一字不变，老用户升级不迁移。
- 新增 WorkBuddy 皮肤 CSS 档案 `src/skin-css-workbuddy.mjs`：同时覆盖 `--wb-*` 结构层、`--cb-*` 语义层、`--cb-vscode-*` 桥接层三层设计令牌；令牌同时声明在 `:root` 和 `body` 上（宿主主题用 `:root, body[data-vscode-theme-name="IDE Light"]` 选择器把整套令牌在 body 又声明了一遍，只写 `:root` 会被盖掉）；只挂稳定选择器，构建哈希类名零出现。
- 状态目录按产品隔离：WorkBuddy 使用 `HeiGeCodexSkinStudio-workbuddy`，两个产品的锁、state.json 与用户主题库互不干扰。

### 安全

- WorkBuddy 这一版只做一次性皮肤，不开控制通道、不支持常驻：它的 renderer 是 `app.asar` 里的本地 `file://` 页面，回调控制服务时带 `Origin: null`，放行等于掏空来源校验；控制服务保持只认 `app://-`。`set-persistence --app workbuddy` 明确报错。

### 修复

- 进程识别不再把宿主的脚本子进程当成主进程候选：Electron 应用会拿同一个主可执行文件跑内部脚本（WorkBuddy 真机上 daemon、sidecar、CLI 预热都是 `Electron <脚本路径> …`），此前会被误判成多实例并拒绝接管。现在以第一个参数判身份，位置参数只有子进程才有，主进程要么零参数要么以 `--flag` 开头。
- 重启续作找回产品身份：分离式重启的续作此前不带 `--app`，WorkBuddy 重启后按 Codex 跑 preflight 必然失败并触发补偿回滚。现在续作从动作文件的 `appPath` 反推产品（动作文件不落产品字段），`workbuddy-apply.command --restart` 全链路真机跑通。

### 已知边界

- WorkBuddy 皮肤在 macOS 真机验证（WorkBuddy 5.3.11，浅色与深色主题均有截图证据）；Windows 侧只写了结构，未在真机验证。
- WorkBuddy 自身不带可执行的 Node，运行入口需要系统 Node.js 22 或更新版本。
- 宿主机在换肤操作中途断电或强杀，状态目录可能留下断裂的锁链并报 `LOCK_CHAIN_CORRUPT`；确认没有本工具进程在跑后，删除状态目录下的 `operation.lock*` 文件即可恢复。

### 测试

- 新增 `test/products.test.mjs`：产品档案事实、状态隔离、CDP 启动方式与皮肤 CSS 不变量断言。
- CLI 层新增 `--app` 闸门用例：未知产品拒绝、WorkBuddy 拒常驻放行只读命令、`HEIGE_SKIN_APP` 回退与显式参数优先级。
- 进程表解析新增回归用例：同一 Electron 可执行文件跑的脚本子进程不算主进程候选（用 WorkBuddy 真机 ps 快照复刻，token 已置零）；重启续作契约断言现在包含 `appPath` 透传。

## 5.4.12 - 2026-08-02

### 修复

- 补回新版 Codex 主内容容器的透明度选择器，避免新版 DOM 下背景透明度回退。
- 健康状态读取不再把 renderer 拉到前台，避免后台巡检抢焦点。

### 性能

- 关闭侧栏、输入框、用户气泡和审批表面的 `backdrop-filter`，避免滚动与流式输出持续采样动态背景。
- 非持久会话的健康巡检走一次轻量无租约路径，减少后台控制器开销。

### 测试

- 增加皮肤 CSS、注入器焦点行为和 retained session 巡检的回归断言。

## 5.4.11 - 2026-07-31

### 修复

- 统一菜单自定义图片的公开契约：新上传会落盘为正式用户主题，只有旧版 `custom-upload` 保留为本地兼容槽。
- 恢复 `llms-full.txt` 同步、确定性 `.skill` 制品与发布哈希一致性。
- 以 `package.json` 作为版本真相源，README 改为不绑定 patch 版本的最新 Release 链接。

### 测试

- `release:check` 现在同时覆盖完整 Node 测试、确定性打包门和公开素材来源门。

## 5.4.6 - 2026-07-19

### 修复

- 修复自定义上传成功后「我的主题」不立刻出现卡片：HTTP/CDP 确认后立刻 upsert 正式用户主题行，不再等 reinject；CDP 排队期间保留本机快捷预览，避免确认前列表空白。

### 测试

- 新增主题中心 HTTP/CDP 上传成功后立刻出现正式「我的主题」卡片（无 `custom-upload` 残留）的 DOM 断言。

## 5.4.5 - 2026-07-19

### 修复

- 修复顶部「皮肤常驻」开启慢/假等待：菜单 HTTP 超时由 3s 提到 15s（与主题保存一致），业务错误（含 `BACKGROUND_START_FAILED` 补偿关闭）立刻告警，不再误排队 CDP 兜底；CDP 兜底超时收至 20s。
- 修复 Windows 开启常驻时前后台抢锁出现 `LOCK_MALFORMED`（staging 缺 `owner.json`）导致开启被补偿回关：锁获取对 `LOCK_MALFORMED` / 瞬时 `LOCK_PERMISSIONS` 做有界重试，清理并发 staging 时跳过未写完的目录，并显式传递 `AppIdentityToken`。
- 修复提升权限会话下计划任务 `RunLevel=Limited` 与 High IL 锁目录冲突导致的 `LOCK_PERMISSIONS` / `BACKGROUND_START_FAILED`：注册进程若已提升，控制器任务改为 `Highest`；Windows 握手等待放宽到 35s。
- 合入 Windows 常驻重启接管：用户正常启动不带 CDP 的 Codex 时，后台控制器会安全退出并以 CDP 重新拉起再注入（独立版/可开 CDP 环境；Store 屏蔽 9341 仍属环境限制）。`restart-into-cdp` 改为附着等待完成并回传错误，避免 detached 子进程脱离交互会话后静默失败。
- 修复 Windows 常驻任务 `Stop-ScheduledTask` 后 `Start-Process` 拉起的 Node 变孤儿、叠成双后台并写旧 handshake 的问题：启停/注销前按 task-name + state-directory 精确杀掉残留 `--background` 控制器；握手等待在「字段不匹配且 PID 已死」时清掉陈旧文档再继续等。
- 修复多后台抢锁导致 apply/`LOCK_DISAPPEARED`：清理 disposable staging 时把并发 rename 造成的消失视为可跳过竞态；锁获取对 `LOCK_DISAPPEARED` 有界重试；后台 Node 启动时再清同 task 兄弟进程（ExcludePid 保留自身）。
- 修复主题中心从自定义快捷图切回正式主题时「无切换中状态 / 像没保存」：自定义顶栏改为「仅本机快捷 · 未写入启动器」；正式主题保存至少亮 450ms「正在保存」并灰态禁用卡片；幂等确认给出「已确认启动器主题」反馈。
- 修复 Windows 稳定树安装 rename 报 `EPERM`：publish 前先停计划任务并杀掉仍占用安装目录的常驻后台 Node；失败文案提示关闭占用进程后重试。
- **行为变更**：主题中心上传自定义图片改为写入用户主题库并 CAS 记入启动器（与 CLI `customize` 同源），常驻/重启可稳定复现；不再把新上传仅存为 `custom-upload` localStorage 快捷槽。
- 修复主题中心上传后仍显示「仅本机快捷 · 未写入启动器」：Codex CSP `connect-src` 禁止 renderer `fetch` 本机控制口，上传只能走 CDP 兜底，但状态提取此前丢掉了 `publish-user-theme` / `delete-user-theme`（现已补齐）；控制口 CORS 同时补上 `Access-Control-Allow-Private-Network`；有控制通道时点击旧本机快捷槽改为写入启动器。
- 修复「我的主题」删除按钮无效：删除同样受 CSP 影响却未排队 CDP；ACK 也不接受 `delete-user-theme`。现与发布同源走 CDP 兜底并在确认后移除卡片；删除热区加大到 32px。
- 修复删除/发布排队后被远程 revision 广播清掉请求却不复位 `themePending`，导致后续删除静默无效；忙碌中再次点击会提示等待。
- 修复常驻 tick 在 session 未完全对齐（`keepUntilProcessExit` / apply 过渡）时跳过 CDP 抽干，导致主题中心删除/发布一直「等待后台确认」：有进程与控制口时优先处理 `controlRequest`。

### 性能

- 保留 5.4.4 的空闲健康巡检 10s；有 relaunch/注入/renderer 控制请求等交互时改用 1s 节拍，避免 CDP 兜底最坏多等一整拍。

### 测试

- 新增常驻超时阈值、业务失败不排队 CDP、双节拍、锁重试与 Windows 握手 35s 等回归断言。
- 新增 Windows 孤儿后台控制器精确清理、启停顺序（stop→orphan→start）与握手清陈旧死 PID 文档等回归断言。


## 5.4.4 - 2026-07-19

### 性能

- 皮肤 CSS 去掉了流式输出场景下最贵的两类渲染负载：背景图不再使用 fixed 附着（滚动/流式 relayout 不再强制整视口逐帧重绘）；侧栏与气泡/输入框/审批面板的 backdrop-filter 模糊半径由 20~22px 降至 8px，气泡底色不透明度由 60% 提升至 80% 以保持可读性。显著缓解流式输出期间的界面卡顿。
- 常驻后台健康巡检节拍由 1 秒放宽到 10 秒：皮肤丢失的感知容忍度为十秒级，原 1Hz 巡检每秒 fork 多个 ps/lsof 进程并完成一次完整 CDP 握手，属于纯持续税。
- macOS 进程探测新增 pid 快速路径：上次确认的 CDP 进程身份未漂移时用单行 `ps -p` 代替全量进程表扫描（lstart+命令行双重校验，漂移自动回退全表）。

### 测试

- 新增回归断言：皮肤背景禁用 fixed 附着、气泡模糊半径上限 8px、后台巡检节拍为 10 秒。

## 5.4.3 - 2026-07-19

### 修复

- 修复 Windows 常驻开启后用户正常重启 Codex 皮肤不恢复的问题：后台控制器此前只在 macOS 接线 `probeNativeProcess` / `restartIntoCdp`，Windows 一直停在 `wait-for-app`。现已通过 PowerShell 安全退出并以 CDP 重新拉起，随后由控制器注入皮肤。

### 新功能

- 新增 Windows 一键关闭入口 `scripts/windows/close-codex.ps1` / `close-codex.bat`：只安全完整退出已归属的 Codex/GPT 桌面进程并保持关闭，不改常驻、不自动 apply、不自动重启；多实例归属不清时 fail-closed。
- `abort-incompatible`（调试端口不可用）引导、CLI 生命周期说明、手册与 Skill 已指向该入口；Skill 仅在用户明确允许关闭时才可调用。

## 5.4.2 - 2026-07-19

### 修复

- 修复主题切换提交期间后台健康检查误触发 reinject，导致主题中心过早关闭、主题未完整切换的问题；切换中的 renderer 现在保持稳定，必要的 reinject 也会恢复弹窗打开状态。
- 新增 Windows 完整卸载入口与安装树缺失自愈，清理当前用户计划任务、开始菜单、AppData 状态、残留控制器进程和稳定安装目录。

## 5.4.1 - 2026-07-19

### 新功能

- 新增 `apply.command --restart` 强制重启入口：先正常退出 Codex 再拉起注入，用于长时间运行后整窗帧率骤降（合成器卡死在约 10 帧，与皮肤无关）这类只有冷重启才能恢复的场景。健康会话下直接重跑 apply 保持幂等，不会重启进程；退出步骤校验进程身份，绝不强杀。

### 性能

- 侧栏 PR 状态点的隐藏规则从父级反向匹配改为直接命中 svg，移除样式表中唯一的反向失效扫描热点，降低流式输出高频 DOM 变更时的样式重算开销，并加入测试门禁防止此类选择器回归。

### 发布说明

- 重建 `output/heige-codex-skin-studio.skill` 打包产物，恢复与当前源码确定性构建哈希一致（5.4.0 发布后的文档图片更新曾使产物过期）。

## 5.4.0 - 2026-07-18

### 新功能

- 新增「龙珠 · 筋斗云」和「龙珠 · 超级赛亚人」两个内置主题预设。
- 两张主视觉均压缩为 1600×900 WebP，单张低于 180 KB，保留左侧阅读安全区与右侧人物焦点。
- 两个预设使用浅色表面和深色文字，兼顾阅读增强底色与 Codex 原生白色信息卡的文字对比。

### 性能

- 新主题只增加静态清单和图片，不增加运行时脚本、依赖、事件监听、网络请求或后台任务。
- 继续复用默认开启的无模糊阅读增强，复杂背景下保持文字可读性，不扩大 GPU 模糊开销。

### 兼容性

- 逐一复核 12 套内置主题，修复深色主题在 Codex 原生白色「环境信息」看板和顶部浅色按钮上出现浅色文字的问题。
- 兼容层只使用窄范围静态 CSS，不覆盖增删统计等语义颜色，不为各主题复制规则。
- 主题缩略图支持有界放大，龙珠双主题的小卡片和顶部圆形入口改为人物脸部特写，大横幅焦点同时校准。

### 发布说明

- 两张图片由项目所有者提供并确认公开发布，角色形象的第三方权利边界已如实登记在 `ASSET_PROVENANCE.md`。

## 5.3.0 - 2026-07-18

### 新功能

- 阅读增强默认开启，为 AI 回复区提供主题自适应半透明底色，并可在主题中心关闭。
- 阅读增强偏好跨重新注入保留，并通过现有 renderer 通道同步，不增加后台接口或滚动监听。

### 修复

- 修复发布包中可执行 `.zsh` 入口被错误打包为 `0644` 的问题。
- 修复阅读增强只覆盖最终回复、导致同一对话中的过程回复仍然透明的问题。
- 蒙版改用统一回复正文节点，增加对称留白并移除大阴影，改善左上角边缘并缩小滚动绘制范围。

## 5.2.5 - 2026-07-18

### 修复

- 修复 Windows PowerShell 5.1 的 Skill 文档契约失败：明确写出仅当用户主动要求安装 Miku Future 时才执行宠物安装。
- 保持皮肤安装与宠物安装相互独立，不因安装或应用皮肤自动启用可选宠物。

### 测试

- 重新执行 Node、macOS lifecycle、Windows PowerShell parity 和可复现 `.skill` 制品四组 GitHub Actions 门禁。
- Windows 门禁要求 PowerShell 5.1、PowerShell 7、32 位解析、确定性制品安装和隔离 Scheduled Task 集成全部实际执行。

### 发布说明

- 本版本包含 5.2.4 的换肤锁竞争修复；安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。

## 5.2.4 - 2026-07-18

### 修复

- 修复后台巡检与菜单换肤请求并发争抢跨进程文件锁，导致部分主题点击偶发不生效的问题。
- 控制器内部的持锁操作改为 FIFO 串行执行，并对其他可信进程造成的短暂 `LOCK_HELD` 增加有限重试；原有安全锁和校验保持不变。
- 重新生成确定性 `.skill` 制品，确保压缩包字节、发布哈希与当前源码完全一致。

### 测试

- 新增巡检与换肤重叠、短暂锁冲突重试、非锁冲突不重试三类回归测试。
- 用户实测连续换肤正常；重启 Codex 后主题、持久化、CDP 和主题菜单均正确恢复。
- Node 全量测试共 985 项，979 项通过、0 项失败、6 项按环境跳过；发布治理测试 10/10 通过。

### 发布说明

- 安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。

## 5.2.3 - 2026-07-18

### 新功能

- 主题中心常驻显示当前安装版本，用户可以手动检查 GitHub 最新正式版。
- 检测到新版本后可一键复制完整更新指令，粘贴给 Codex 即可继续更新。

### 文档与社区

- README 重构为首页样式：真机大图前置，工程细节移入 `docs/manual.md`，英文文档拆分为 `README.en.md`。
- 新增 `docs/theme-prompts.md`：8 套可直接复制的主题背景生图提示词与晒图指引。
- 新增 `docs/promo-copy.md`：按平台整理的转发文案包。
- 新增 Bug、功能建议、主题晒图三个 Issue 模板与 PR 模板，关闭空白 Issue，开启 Discussions 晒图区。

### 修复

- 修复换肤后主题中心的全屏交互层覆盖 Codex 原生标题栏，导致顶部空白横条无法拖动窗口的问题。

### 发布说明

- 已在当前 Mac 上重新安装本版本，并真实验证 Miku 主题下顶部横条拖动和主题入口点击。
- 安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。

## 5.2.2 - 2026-07-18

### 性能优化

- 后台控制器将主题请求检测与健康巡检合并为一次 renderer 扫描，减少常驻检查开销，同时保持一秒内响应主题点击。
- 主题卡片继续采用点击即切换，不增加额外保存按钮或二次确认步骤。

### 修复

- 修复 Codex renderer 无法直连控制端点时，后台巡检漏读排队请求并将内置主题错误恢复成 Miku 的问题。
- 修复主题中心「当前主题」看板随机裁切人物的问题，各内置主题现在使用独立主体焦点，优先保留人物脸部特征。
- 自定义图片在当前主题看板中改为完整展示，避免上传图片的主要内容被横向裁掉。
- 恢复字体颜色处理说明中的 👉 和 ✅ 引导符号。

### 发布说明

- 已在当前 Mac 上真实验证 Miku 与原神主题往返点击切换，后台确认后均保持正确主题。
- 安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。

## 5.2.1 - 2026-07-17

### 新功能

- 内置主题会随皮肤自动同步 Codex 的浅色、深色或系统外观。
- 菜单上传的自定义图片会根据画面亮度自动选择 Codex 深浅外观，旧版快捷图片也能兼容补判。
- 主题面板新增字体颜色异常处理说明，可按「左下角头像 → 设置 → 外观」手动调整浅色或深色主题。
- 发布包内继续自带可选的 `Miku Future` 动画 Pet，可独立安装，不覆盖 Codex 内置宠物。

### 性能优化

- 主题点击后立即完成当前 renderer 渲染，Codex 真机实测画面切换约 52 ms。
- renderer 无法直连本机控制端点时，持久化请求改为后台排队，主题卡片保持可操作。
- 快速连续切换主题时合并等待中的请求，仅持久化最后一次选择。

### 修复

- 修复 CSP 阻止 renderer 请求控制端点后，主题按钮长时间锁定并产生约 2 至 3 秒可感知延迟的问题。
- 修复深色与浅色图片切换后，Codex 设置中的外观没有同步变化的问题。

### 发布说明

- 安装包内含可选 `Miku Future` Pet，皮肤安装不会自动启用 Pet。
- 安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。

## 5.1.0 - 2026-07-17

### 新功能

- 全新「极光画廊」主题中心，支持主题缩略图预览、原生界面恢复和自定义图片入口。
- 主题中心与左侧菜单采用统一的玻璃质感和交互层级。

### 性能优化

- 主题切换改为先完成当前界面渲染，再在后台持久化状态，显著缩短可感知等待时间。
- 健康巡检不再反复进入耐久锁，压缩生产锁链并降低长期运行时的 CPU 占用。

### 修复

- 修复助手消息区域出现锐利白边、丢失半透明效果的问题。
- 修复切换到原生界面后被后台巡检重新切回 Miku 主题的问题。
- 修复多个对话重复展示相同 Git 和 PR 状态图标的问题。
- 修复 macOS 首次安装状态目录、Codex 精确启动和失败阶段诊断。
- 修复 Windows 计划任务身份归一化与跨 PowerShell 兼容问题。
- 修复自定义桌宠需要重启时缺少明确提示的问题。

### 发布说明

- 安装包继续使用本机回环 CDP，不修改 Codex 的 `app.asar`、应用二进制或签名资源。
- 项目所有者已确认当前视觉素材随本版本公开发布。素材权利边界与来源记录见 `ASSET_PROVENANCE.md` 和 `NOTICE.md`。
